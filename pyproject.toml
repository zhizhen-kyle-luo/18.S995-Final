[project]
name = "18-s995"
version = "0.1.0"
description = "Numerical diagnostics comparing core LayerNorm and core Dynamic Tanh on DistilBERT hidden states (18.S995 final project)."
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
    "torch>=2.11.0",
    "tqdm>=4.67.3",
    "transformers>=5.7.0",
]
